
  # Landing Page for ELI Studio

  This is a code bundle for Landing Page for ELI Studio. The original project is available at https://www.figma.com/design/xIbQrxQqN0ifEZ09kAlvHS/Landing-Page-for-ELI-Studio.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  